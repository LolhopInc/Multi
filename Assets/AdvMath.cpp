#include<cstdlib>
#include<cmath>

using namespace std;

struct roots {
float r1;
float r2;
}
roots quadratic(float a, float b, float c) {
struct roots r;
r.r1 = -b + sqrt(b * b - 4 * a * c) / 2 * a
}
